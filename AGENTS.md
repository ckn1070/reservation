# AGENTS.md

이 파일은 이 저장소에서 AI 작업자가 따라야 하는 공통 작업 규칙입니다.
상세한 배경과 운영 방식은 `docs/000-index.md`에서 시작해 확인합니다.

## Core Principle

- 사용자의 요청은 고정된 구현 지시가 아니라, 달성하고자 하는 목표와 아이디어로 해석합니다.
- 제시된 방법을 그대로 실행하기 전에, 목표 달성에 더 나은 방법이 있는지 먼저 검토합니다.
- 범위, 구조, 유지보수성, 보안, 데이터 무결성에 의미 있는 영향을 주는 대안이 있으면 구현 전에 사용자에게 먼저 제안합니다.
- 단순한 세부 구현이나 명백히 동등한 선택지는 기존 코드와 문서 규칙에 맞춰 실용적으로 진행합니다.
- 사용자와 논의하면서 최선의 방법을 찾는 것을 기본 협업 방식으로 둡니다.

## Language

- 내부 사고 언어는 자유롭게 선택할 수 있습니다.
- 사용자에게 보이는 응답, 보고, 문서는 기본적으로 한국어로 작성합니다.
- 사용자에게 답변할 때는 항상 존댓말을 사용합니다.

## Documentation Routing

- 작업이 프로젝트 규칙, 워크플로우, 구조, 도메인 지식에 영향을 받을 수 있으면 `docs/000-index.md`를 먼저 확인합니다.
- 작업 워크플로우, 설계 절차, 개발 절차, 커밋 방식은 `docs/workflow/000-index.md`를 진입점으로 사용합니다.
- 제품 목표, 기능 범위, 보류 범위는 `docs/product/000-index.md`를 진입점으로 사용합니다.
- 프로젝트 실행, 설정, 운영 전제는 `docs/project/000-index.md`를 진입점으로 사용합니다.
- 도메인 기능, 상태 전이, 트랜잭션 흐름은 `docs/domain/000-index.md`를 진입점으로 사용합니다.
- API 엔드포인트와 에러 코드는 `docs/api/000-index.md`를 진입점으로 사용합니다.
- DB 스키마와 마이그레이션은 `docs/database/000-index.md`를 진입점으로 사용합니다.
- 아키텍처, 계층 경계, 패키지 구조, 의존성 방향은 `docs/architecture/000-index.md`를 진입점으로 사용합니다.
- 기술 선택, 프레임워크 사용, 라이브러리 관례는 `docs/tech-stack/000-index.md`를 진입점으로 사용합니다.
- 기능, 버그, 개선, spike의 작업 이력은 `docs/work-items/000-index.md`를 진입점으로 사용합니다.
- 상세 문서는 관련 `000-index.md`를 통해 필요한 문서만 선택해서 읽습니다.
- 모든 문서를 기본으로 전체 스캔하지 않습니다. 먼저 목차를 읽고, 현재 작업에 필요한 상세 문서만 확인합니다.
- 필요한 규칙 문서가 없으면 기존 코드와 저장소 관례를 우선 따르고, 반복될 가능성이 있는 내용은 문서 추가 또는 갱신을 제안합니다.

## Work Process

- 변경 전 관련 코드, 문서, 현재 작업 상태를 확인합니다.
- 변경 범위는 요청한 목표를 달성하는 데 필요한 만큼으로 유지합니다.
- 사용자가 만든 기존 변경 사항은 되돌리지 않습니다.
- 불필요한 리팩터링, 파일 이동, 포맷 변경을 피합니다.
- 임시방편보다 원인을 해결하는 방식을 우선합니다.
- 사용자가 명시적으로 요청하지 않으면 커밋을 만들지 않습니다.

## Development Rules

- 기능 추가, 버그 수정, 도메인 로직 변경은 TDD를 기본 방식으로 진행합니다.
- TDD 작업은 `docs/workflow/210-tdd-workflow.md`를 기준으로 진행합니다.
- TDD가 적절하지 않거나 먼저 적용하기 어려운 경우에는 이유와 대안을 사용자에게 먼저 제시합니다.
- 문서, 단순 설정, 탐색용 spike처럼 TDD 예외에 해당하는 작업은 적절한 검증 방식으로 대체합니다.

## Architecture Rules

- 이 프로젝트는 bounded context별 기능 우선 패키지 구조 안에서 Clean Architecture를 지향합니다.
- 도메인과 유스케이스는 Web, DB, Security 같은 외부 기술 의존성을 피합니다.
- 구조 변경이나 계층 경계 판단이 필요하면 `docs/architecture/000-index.md`를 먼저 확인합니다.
- Clean Architecture 적용이 과도하거나 부적절하면 이유와 대안을 사용자에게 먼저 제시합니다.
- 명확한 선택지가 없으면 기술적 완결성, 유지보수성, 확장성, 성능, 개발 속도를 비교합니다.

## Tech Stack Rules

- 기술 선택이나 프레임워크 관례가 중요한 작업은 `docs/tech-stack/000-index.md`를 먼저 확인합니다.
- 기본값은 해당 기술의 공식 문서와 현대적 Best Practice를 따릅니다.
- Best Practice와 프로젝트 제약이 충돌하면 트레이드오프와 대안을 사용자에게 먼저 제시합니다.
- Spring Boot, Security, JPA, Flyway, MySQL, OpenAPI, 테스트 관련 작업은 해당 기술 문서를 확인합니다.

## Commit Rules

- 커밋이 필요한 작업은 `docs/workflow/300-commit-workflow.md`를 기준으로 진행합니다.
- 커밋 단위는 기능 또는 의미 있는 변경의 최소 완결 단위로 나눕니다.
- 커밋 메시지는 Conventional Commits 형식을 따르고, 한 줄 요약을 기본으로 합니다.
- 커밋 메시지에는 핵심 변경 내용만 압축해서 담고, 불가피할 때만 본문을 추가합니다.
- `Co-authored-by` footer는 추가하지 않습니다.

## Validation And Reporting

- 변경 후 가능한 범위에서 가장 관련성 높은 검증을 우선 실행합니다.
- 검증을 실행하지 못했거나 실행하지 않는 편이 적절하면 그 이유를 보고합니다.
- 완료 보고에는 핵심 변경 내용, 변경 파일, 검증 결과, 남은 리스크를 간결하게 포함합니다.

## 변경 로그

### 2026-06-05

- product, work-items, 설계 워크플로우 문서 그룹을 문서 라우팅에 추가했습니다.

### 2026-06-04

- 가져온 AI 작업 규칙을 reservation 문서 체계에 맞게 조정했습니다.
- reservation 전용 문서 라우팅, bounded context 구조, MySQL 기준을 추가했습니다.
